-- VSCode Neovim entry point (loaded only when vim.g.vscode == 1).
-- Module name `vscode_config` is used because `vscode` is reserved by
-- the vscode-neovim runtime.

require "vscode_config.options"
require "vscode_config.mappings"
