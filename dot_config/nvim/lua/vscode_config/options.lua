-- Loaded only when running inside VSCode Neovim (vim.g.vscode == 1).
-- Keep this minimal: visual / UI options are handled by VSCode itself.

vim.opt.clipboard = "unnamedplus"
vim.opt.ignorecase = true
vim.opt.smartcase = true
vim.opt.scrolloff = 8
