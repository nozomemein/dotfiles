# Shared aliases (managed by chezmoi)
# Local config: user, signingkey, etc. (not managed by chezmoi)
[include]
	path = ~/.config/git/alias.conf
	path = ~/.config/git/delta.conf
	path = ~/.gitconfig.local

[rebase]
	autoSquash = true
	autoStash = true

[commit]
	verbose = true
