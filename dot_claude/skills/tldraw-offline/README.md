<!-- installed-by:tldraw-desktop-agent-skills -->
# tldraw agent skill

The shared skill is installed at:

```text
~/.claude/skills/tldraw-offline/SKILL.md
```

Agents should read that file before touching open tldraw Desktop canvases. Vendor-specific copies or pointers may also be installed under `~/.codex`, `~/.claude`, `~/.cursor`, `~/.gemini` (Gemini CLI and Antigravity), and `~/.pi/agent`.
